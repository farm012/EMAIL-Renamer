wtv it just renames your emails if it finds any file in your directory with the .eml or .msg renames it and puts it into .txt file with prefered format... had to reconfugure that too
# have a good day